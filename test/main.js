/*global describe, it*/
"use strict";

var fs = require("fs"),
	should = require("should");

require("mocha");

var gutil = require("gulp-util"),
	gitmodified = require("../");

describe("gulp-gitmodified", function () {

	// tests are comming...

});
